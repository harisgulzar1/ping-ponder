OPENAI_API_KEY=your_api_key

# Optional. Model used by the Ponder reasoning agent. Default: gpt-4.1
# PONDER_MODEL=gpt-4.1

# Optional. Simulated latency in ms for the stubbed webSearch tool, which stands
# in for a real search API. Set to 0 to remove it. Default: 500
# PONDER_SIMULATED_TOOL_LATENCY_MS=500
